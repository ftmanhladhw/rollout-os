# Example environment file. Copy to `.env` and fill in real values.
# `.env` is git-ignored; never commit real secrets.
#
# Where these come from: SETUP.md (Supabase project → API keys, connection strings).
# Validated at boot by src/lib/env.ts — all optional in dev, required in production.
# For CI/lint steps that don't touch the DB or auth, set SKIP_ENV_VALIDATION=true.

# --- Database (Supabase Postgres) ---
# Pooled connection (PgBouncer) used by the app at runtime.
DATABASE_URL="postgresql://postgres:[PASSWORD]@[HOST]:6543/postgres?pgbouncer=true"
# Direct connection used by Prisma for migrations.
DIRECT_URL="postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres"

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[ANON-KEY]"
# Server-only. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY="[SERVICE-ROLE-KEY]"

# --- App ---
# Canonical site origin; base URL for auth email redirects.
# Production: set to the deployed domain, e.g. https://rollout-os.vercel.app
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
